# 강의자료 공개 사이트 (lecture-slides)

순천향대학교 전자정보공학과 강의자료를 **HTML로 만들어 웹에 공개**하는 프로젝트다.
학생들이 주소만 알면 브라우저에서 바로 볼 수 있고, 필요하면 PDF로 받아 태블릿에서 필기한다.

- 공개 주소: <https://goodgodgd.github.io/lecture-slides/>
- 저장소: `goodgodgd/lecture-slides` (public)
- 로컬 경로: `F:\work\lecture\lecture-slides\`

## 0. 이 폴더는 바깥 저장소와 별개다 — 섞지 않는다

이 폴더는 강의 운영 저장소(`lecture-manager`, private) **안에 들어와 있지만 독립된 git 저장소**다.
바깥 `F:\work\lecture\.gitignore`에 `lecture-slides/`가 등록돼 있어 서로 간섭하지 않는다.

| 무엇 | 어디에 | 공개 여부 |
|---|---|---|
| 학생 성적·제출물·채점 결과 | `lecture-manager` (바깥) | **비공개** |
| 강의자료 HTML·PDF | `lecture-slides` (여기) | 공개 |
| 강의자료 원본 (노션 등에서 받은 md) | `{과목}/_inbox/` (여기) | **git 미추적** |

원본 md는 이 저장소 안에 있지만 `.gitignore`의 `_inbox/` 규칙으로 커밋되지 않는다.
내 손이 닿는 위치에 두되 웹에는 올리지 않기 위한 배치다.

**절대 이 저장소에 넣지 않는 것:** 학번, 이름, 점수, 제출 파일, 채점 결과.
전부 공개되므로 개인정보가 하나라도 섞이면 즉시 노출된다. 커밋 전 반드시 확인한다.

## 1. 배포는 push 한 번으로 끝난다 — 빌드 설정이 없다

`main` 브랜치의 루트를 GitHub Pages가 그대로 서빙한다. 별도 빌드 도구도, 정적 사이트 생성기도 쓰지 않는다.
**HTML 파일이 곧 최종 결과물이다.** `main`에 반영되고 약 1분 뒤 웹에 뜬다.

다만 **`main`에서 바로 작업하지 않는다.** 브랜치에서 만들고 병합해야 배포된다 (2절).

- **`.nojekyll` 파일을 지우지 않는다.** 이 파일이 없으면 GitHub이 `_`로 시작하는 파일·폴더를
  웹에서 감춰 버린다.
- **폴더·파일 이름은 영문 소문자**로 짓는다. 한글 이름은 주소창에서 `%EB%A7%88...` 형태로 깨져 보인다.
  (예: `robotics/`, `micro/`, `w01.html`)

## 2. 작업은 브랜치에서 한다 — `main`에 바로 쓰지 않는다

`main`은 학생이 지금 보고 있는 화면이다. 반쯤 쓴 자료가 올라가면 그대로 공개된다.
그래서 **모든 작업은 브랜치에서 시작한다.** 예외는 오타 하나 고치는 정도다.

### 브랜치 이름은 대충 짓고 나중에 고친다

작업을 시작할 때는 무엇을 어디까지 할지 모르는 게 보통이다.
**이름을 고민하느라 시작을 미루지 않는다.** 과목명만 넣어 아무렇게나 만들고 들어간다.

```bash
git switch -c robotics-wip        # 시작할 땐 이 정도면 된다
```

push하기 직전에, **실제로 한 일에 맞춰 이름을 고쳐서 올린다.**

```bash
git branch -m robotics-l06-l08    # 지나고 보니 6~8회차를 만들었다
git push -u origin robotics-l06-l08
```

이름 규칙은 `{과목}-{한 일}`이다. 영문 소문자와 붙임표만 쓴다.
(예: `robotics-l06-l08`, `robotics-css-fix`, `micro-w03`)

### 커밋은 내가 알아서, 병합은 사용자에게 권한다

세 줄 중 마지막 줄만 사용자 몫이다. 나머지는 묻지 않고 진행한다.

| 무엇을 | 누가 | 언제 |
|---|---|---|
| 커밋 | 에이전트가 알아서 | 한 덩어리가 끝날 때마다 (자료 한 편, 스타일 한 벌) |
| push | 에이전트가 알아서 | 브랜치 이름을 고친 뒤 |
| PR 만들고 병합 | **사용자가 결정** | 에이전트가 때가 됐다고 판단하면 **권유한다** |

- 커밋 메시지는 한글로, 무엇이 왜 바뀌었는지 쓴다. 커밋할지 말지를 사용자에게 묻지 않는다.
- **병합은 혼자 하지 않는다.** 대신 아래 자리에서 사용자에게 권한다.
  - 자료 한 편이 검증까지 끝나 공개해도 되는 상태가 됐을 때
  - 한 부(예: 1부 5편)가 통째로 끝났을 때
  - 브랜치가 열흘쯤 묵어 `main`과 벌어지기 시작할 때
  - 사용자가 "이제 올리자"에 해당하는 말을 했을 때
- 권할 때는 **무엇이 공개되는지 한 줄로 요약**하고 명령까지 같이 낸다.
  "6~8회차 3편이 준비됐습니다. 병합하면 학생이 볼 수 있습니다. `gh pr create ...`"

## 3. 자료 형식은 두 가지이고, 과목마다 하나를 고른다

과목의 성격에 따라 형식이 다르다. **섞어 쓰지 않는다.** 새 과목을 시작할 때 사용자에게 어느 쪽인지 확인한다.

### 3-1. 문서형 — 스크롤되는 긴 웹페이지

교재처럼 글이 이어지는 형식이다. 코드 예제와 실습 안내가 많은 과목에 쓴다.

- **적용 과목: 로봇공학** (`robotics/`)
- 한 강의 = HTML 파일 하나. 페이지를 끊지 않고 세로로 길게 흐른다.
- PDF는 만들지 않는다. 웹에서 읽는 것을 전제로 한다.
- 원본은 `robotics/_inbox/`에 있는 노션 내려받기 md 파일 32개다.
  그림 90장은 같은 폴더의 `figures/`에 있다.
- `robotics/src/notion_download.py`는 노션에서 원본을 받아온 스크립트다.

### 3-2. 슬라이드형 — A4 가로 한 장씩 끊어진 페이지 + PDF

수업 중 투사하고 학생이 태블릿에서 필기하는 형식이다. **아직 쓰는 과목이 없다.**
사용자가 대상 과목을 지정하면 그때 서식을 만든다.

- 페이지 규격: **A4 가로 297×210mm** (약 1.4:1)
  → 아이패드(4:3)·갤럭시탭(16:10) 화면을 가장 꽉 채우고 종이 인쇄도 그대로 된다.
  16:9 슬라이드는 태블릿에서 위아래 여백만 커지므로 쓰지 않는다.
- 한 페이지 = `<div class="page">` 하나. **처음부터 페이지 단위로 만든다.**
  세로로 흐르는 문서를 나중에 PDF로 바꾸면 문단 한가운데서 잘린다.
- **PDF를 반드시 함께 배포한다.** 학생들이 쓰는 필기 앱(굿노트·노타빌리티·삼성 노트)은
  PDF만 받는다. 웹페이지에는 펜으로 밑줄을 그을 수 없다.
  HTML 원본 하나에서 PDF를 자동 생성하며, 파일을 두 벌 관리하지 않는다.
- **필기 여백은 과목마다 정한다.** 여백 있는 판과 없는 판을 CSS 클래스 하나로 바꿔 끼운다.
  새 과목 시작 시 사용자에게 묻는다.
- 각 페이지 상단에 "PDF 내려받기" 링크를 단다.

## 4. 용량은 넉넉하지만 PDF 수정 횟수를 조심한다

GitHub Pages 무료 한도는 저장소 1GB, 월 전송량 100GB다. 네 과목을 한 학기 올리면 약 120MB이니
8학기(4년)를 쌓아야 한도에 닿는다. 학생 100명이 전부 내려받아도 전송량은 한도의 8분의 1 수준이다.

**진짜 변수는 용량이 아니라 수정 횟수다.** PDF는 통짜 바이너리라 오타 하나를 고쳐 다시 커밋해도
git이 파일 전체를 새로 저장한다. 같은 자료를 열 번 손보면 저장소에 열 벌이 쌓인다.

지금은 PDF를 그대로 커밋한다. 저장소가 무거워지면 그때 GitHub Actions에서 HTML→PDF를 변환해
배포 브랜치에 강제 푸시하는 방식으로 옮긴다. 나중에 옮겨도 버리는 작업이 없다.

## 5. 표준 폴더 구조

```
lecture-slides/
├── CLAUDE.md        ← 이 문서
├── .nojekyll        ← 지우지 않는다
├── index.html       ← 전체 과목 목록 (자료를 추가하면 여기 링크도 추가)
├── robotics/        ← 로봇공학 (문서형)
│   ├── _inbox/      ← 원본 md·그림 (git 미추적, 웹에 안 올라감)
│   ├── src/         ← 원본 수집 스크립트
│   └── w01.html     ← 배포되는 강의자료
└── micro/           ← 마이크로프로세서
    └── w01.html
```

과목 폴더는 **`_inbox/`(입력)와 HTML(출력)을 한자리에 둔다.** 원본을 보며 변환 작업을 하기 위해서다.
`_inbox/`는 커밋되지 않으므로 웹에는 HTML만 올라간다.

- 새 자료를 만들면 **반드시 `index.html`에 링크를 추가**한다. 목록에 없으면 학생이 찾지 못한다.
- 파일명은 `w01.html`, `w02.html`처럼 주차 두 자리로 통일한다.

## 6. 프롬프트 기록

이 저장소에서 강의자료를 만드는 작업은 `lecture-slides/prompts.md`에 기록한다.
형식은 바깥 프로젝트 규칙(`F:\work\lecture\CLAUDE.md`의 「프롬프트 기록 규칙」)을 따른다.

## 7. 아직 정하지 않은 것

- 슬라이드형 표준 서식(HTML/CSS)과 PDF 변환 스크립트 — 대상 과목이 정해지면 만든다.
- 로봇공학 md → HTML 변환 방식 (수작업 / 스크립트 / 혼합) — 첫 편을 변환해 보고 정한다.
- 검색·주차별 색인 등 사이트 편의 기능 — 자료가 쌓인 뒤 판단한다.
